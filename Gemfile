source "https://rubygems.org"

# Local preview toolchain. NOTE: the live site is built by GitHub Pages' own
# environment; only whitelisted plugins (jekyll-seo-tag, jekyll-sitemap) are used,
# so the default Pages build stays compatible.
gem "jekyll", "~> 4.3"
gem "jekyll-seo-tag"
gem "jekyll-sitemap"

# Stdlib gems unbundled from Ruby 3.4+/4.0.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
gem "webrick"
